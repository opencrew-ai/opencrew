{
  "name": "opencrew",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "seed": "pnpm --filter @opencrew/server run seed"
  },
  "engines": {
    "node": ">=20"
  }
}
